import Card from "../../../components/Card";

function SchedulerCard() {
  return (
    <Card title={"Scheduler"} to={"/projects/scheduler"}>
      Scheduler Explaination
    </Card>
  );
}

export default SchedulerCard;
